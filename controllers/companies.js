const knex = require("../db/config");
const auth = require("../auth");

/* 
    View all companies
    Business Logic:
    1. Get all the companies in the database including the job postings.
    2. Display the data.
*/
module.exports.viewAllCompanies = () => {

};